{
  "name": "fullstack-template",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "npm --prefix project-web run dev",
    "dev:all": "concurrently \"npm --prefix project-api run dev\" \"npm --prefix project-web run dev\"",
    "dev:api": "npm --prefix project-api run dev",
    "dev:web": "npm --prefix project-web run dev",
    "dev:types": "npm --prefix project-types run dev",
    "dev:electron": "npm --prefix project-web run electron",
    "dev:cli": "npm --prefix project-api run dev.cli",
    "build:types": "npm --prefix project-types run build"
  }
}
